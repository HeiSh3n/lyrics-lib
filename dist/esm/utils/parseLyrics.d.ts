import type { ParsedLyrics } from '../types.js';
/**
 * Parses a lyrics string into synced and unsynced lines.
 * - Synced lines have [mm:ss.xx] timestamps.
 * - Unsynced lines are plain text.
 */
export declare function parseLyrics(raw: string): ParsedLyrics;
//# sourceMappingURL=parseLyrics.d.ts.map